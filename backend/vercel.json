{
  "version": 2,
  "builds": [
    {
      "src": "dist/main.js",
      "use": "@vercel/node"
    }
  ],
  "scripts": {
    "build": "npm run build"
  },
  "routes": [
    { "src": "/(.*)", "dest": "dist/main.js" }
  ]
}
